# The URL the site will be built for
base_url = "https://derik.dev"
theme = "no-style-please"
title = "derik.dev"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true
generate_feed = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true
taxonomies = [
    { name = "tags" },
    { name = "categories" },
    { name = "contexts" },
]


# [taxonomies]
# tags = [ 'engineering', 'training', 'kratix', 'platform-engineering' ]
# categories = [ 'programming', 'running' ]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
list_pages = true
header_nav = [
    { name = "~home", url = "/" },
    { name = "#tags", url = "/tags" },
    { name = "+categories", url = "/categories" },
    # { name = "@contexts", url = "/contexts" },
]
footer_nav = [
    { name = "github", url = "https://github.com/kirederik" },
    # { name = "webring", url = "#" },
    # { name = "next >", url = "#" },
]

# Put all your custom variables here
# links = [
#     { title = "Link display text", href = "http://example.com" },
#     # ...
# ]

